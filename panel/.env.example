# Not: Bu dosya commit'e GEÇMEZ (gitignore'da). Gerçek değerler `panel/.env`'e yazılır.
# Backend adresi — boş bırakılırsa aynı-origin /api kullanılır
# (dev: vite proxy · prod: Caddy /api proxy). Backend ayrı bir domain'deyse doldur:
VITE_API_BASE_URL=https://panel-api.folyo.app