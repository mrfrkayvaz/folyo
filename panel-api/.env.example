# Şema sahibi panel-api: migration (init_db) + admin seed (seed_admin) burada.
DATABASE_URL=postgresql+asyncpg://folyo:folyo@localhost:5432/folyo
CHROMA_DIR=chroma_data

# Admin seed: `uv run python -m app.seed_admin` ile users tablosuna idempotent eklenir.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=degistir-ben